# 开发日志 — 2026-06-21

## 今日完成
- [x] 链接类型解析服务 `src/services/link-parser.ts`
  - 自动识别 YouTube（标准 / Shorts / embed / youtu.be）
  - 自动识别 B站（BV号 / AV号 → player.bilibili.com 嵌入）
  - 自动识别抖音 / TikTok（不支持嵌入，给出友好提示）
  - 自动识别直链 .mp4
- [x] 首页 HomePage 重写：
  - 本地文件上传 → IndexedDB 持久化（不再依赖 sessionStorage）
  - 链接粘贴 → 智能解析 → 生成嵌入 URL → 写入 IndexedDB
  - 不支持嵌入的平台显示红色错误提示
  - 导入中状态禁用按钮
- [x] 学习页 LearnPage 重写：
  - 从 IndexedDB 读取视频信息（兼容 sessionStorage 旧数据）
  - 视频信息卡片：标题、来源标签（本地/在线）、状态标签（彩色）
  - 错误状态 + 空状态完整处理
  - 平台提示（如 B站 可能需浏览器中播放）
  - 本地视频：video 标签 + playsInline + preload
  - 在线视频：iframe 嵌入（allowFullScreen）
- [x] TypeScript 编译通过（33 modules → 362KB）
- [x] PWA manifest + SW 自动生成

## 待办事项
- [ ] 第 3 步：ffmpeg.wasm 音频提取
  - [ ] 安装 @ffmpeg/ffmpeg + @ffmpeg/util
  - [ ] ffmpeg.wasm 加载与初始化
  - [ ] 视频 → WAV 音频提取
  - [ ] 进度展示
  - [ ] 错误处理
  - [ ] 写开发日志 devlog/2026-06-22.md

## 遇到的问题
- npm 工作目录问题：`npx vite build` 需要在项目目录执行，否则报 UNRESOLVED_ENTRY
- Vite 8.x 使用 rolldown（非 Rollup），构建行为略有差异但不影响功能
- 本地视频 blob URL 在页面刷新后失效，后续需考虑持久化策略（或重新从 input 读取）

## 下一步
- 第 3 步：ffmpeg.wasm 音频提取
