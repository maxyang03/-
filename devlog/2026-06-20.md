# 开发日志 — 2026-06-20

## 今日完成
- [x] IndexedDB 数据库初始化（Dexie.js）：5 张表（videos / segments / apiKeys / ttsCache / progress）
- [x] Web Crypto API 加密封装（AES-GCM + PBKDF2 密钥派生 + 设备指纹）
- [x] `useApiKeys` hook：加载→解密 / 修改 / 保存→加密 / 三组连接测试
- [x] DeepSeek 连接测试：GET /v1/models 验证 API Key
- [x] 腾讯云 ASR 连接测试：V3 TC3-HMAC-SHA256 签名 + DescribeTaskStatus 请求
- [x] Azure TTS 连接测试：发送最简单 SSML（"你好"），验证 Subscription Key + Region
- [x] SettingsPage 完整交互：输入框绑定状态、连接状态 Badge、保存反馈动画、链接到各平台获取 Key 页面
- [x] TypeScript 编译通过 + Vite build 成功（32 modules → dist/）
- [x] PWA SW + manifest 自动生成验证通过

## 待办事项
- [ ] 第 2 步：视频导入模块
  - [ ] VideoImport 组件（首页已有基本版本，待完善）
  - [ ] VideoPlayer 组件（本地 video + 在线 iframe）
  - [ ] 链接类型自动识别（B站 / YouTube / 抖音 / 通用）
  - [ ] 视频格式校验扩展
  - [ ] 写开发日志 devlog/2026-06-21.md

## 遇到的问题
- npm run build 需要在项目目录下执行（`cd /Users/max/粤语学习软件`），否则找不到 package.json
- Vite 8.x 使用 rolldown 构建，与之前版本行为略有不同，但不影响功能
- 腾讯云连接测试返回的错误信息需要正确判断（AuthFailure = key 不对，其他错误 = 签名通过）

## 下一步
- 进入第 2 步：完善视频导入模块（本地文件上传 + 在线链接 + 播放器）
