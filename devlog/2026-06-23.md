# 开发日志 — 2026-06-23

## 今日完成
- [x] `src/services/tencent-asr.ts` 完整重写：
  - TC3-HMAC-SHA256 签名（Web Crypto API）
  - `createRecTask()` — 创建录音文件识别任务（音频 base64 直传）
  - `describeTaskStatus()` — 查询任务状态
  - `pollTask()` — 轮询直到 success/failed（最长 2 分钟，间隔 2 秒）
  - `parseResult()` — 解析 ResultDetail → SubtitleSegment[]
  - `transcribeAudio()` — 完整流水线（创建 → 轮询 → 解析 → 存 IndexedDB）
- [x] `src/hooks/useApiKeyForServices.ts` — 为流水线页提供 API Key 读取
- [x] 学习页 LearnPage 完整流水线集成：
  - 步骤 1：🎙️ **提取音频** → ffmpeg.wasm 转 WAV
  - 步骤 2：🔊 **语音识别** → 发腾讯云 ASR → 进度条 + 轮询计数
  - 结果展示：识别出的句子列表（时间戳 + 文本，最多显示 20 句预览）
  - 错误重试 + 重新提取
  - 数据库状态实时同步
- [x] TypeScript 编译通过 + Vite build 成功（45 modules → 379KB, 0 warnings）

## 待办事项
- [ ] 第 5 步：DeepSeek 翻译 + 拼音
  - [ ] 完善 deepseek.ts：翻译请求
  - [ ] Prompt 调优（口语化粤语 + 准确粤拼）
  - [ ] JSON 解析容错
  - [ ] 学习页翻译按钮 + 进度
  - [ ] 写开发日志 devlog/2026-06-24.md

## 遇到的问题
- Vite 构建警告 `[INEFFECTIVE_DYNAMIC_IMPORT]`：crypto.ts 被两个 hook 同时静态导入，改回直接 import 解决
- npm 全局 npx 找不到项目 index.html → 必须 `cd` 到项目目录

## 下一步
- 第 5 步：DeepSeek 翻译 — 将 ASR 结果逐句翻译为粤语 + 粤拼
