# 开发日志 — 2026-06-25

## 今日完成
- [x] `src/services/azure-tts.ts` 完整重写：
  - `synthesizeSpeech()` — SSML POST → MP3 Blob（ssml prosody rate="0.9" 微调语速）
  - `synthesizeWithCache()` — SHA-256 hash 查 IndexedDB 缓存，命中直接返回
  - `synthesizeAll()` — 逐句生成 + 进度回调 + AbortSignal 支持
  - `concatBlobs()` — AudioContext 解码 → 拼接 Buffer + 句间静音 → 编码 WAV
  - 手动 WAV PCM16 编码器（DataView 实现）
  - 三种粤语语音可选（晓曼/晓佳/云龙）
- [x] `src/hooks/useAudioPlayer.ts` — 音频播放控制 hook：
  - `playFull()` — 整段模式：逐句 TTS → AudioContext 拼接 → 播放
  - `playSentence(index)` — 逐句模式：单句 TTS → 播放
  - `pause()` / `resume()` / `stop()` — 基础控制
  - `changeRate(0.5|0.75|1|1.25|1.5)` — 实时调速
  - 进度追踪（timeupdate → 0-100%）
- [x] 学习页 LearnPage 底部播放器完整集成：
  - 整段/逐句模式切换
  - 5 档速度选择（横向滚动）
  - 播放进度条
  - ▶/⏸ 播放暂停 + ⏮⏭ 上下句
  - 逐句模式下点击字幕自动播放该句
  - 加载中动画 + 状态文字提示
  - 模式切换自动停止当前播放
- [x] TypeScript + Vite build 通过（49 modules → 393KB，0 错误 0 警告非构建）

## 待办事项
- [ ] 第 8 步：学习记录 + 打磨
  - [ ] 首页历史记录列表（从 IndexedDB 加载）
  - [ ] 学习进度保存/恢复
  - [ ] 删除记录
  - [ ] "添加到主屏幕"引导提示
  - [ ] 全局错误边界
  - [ ] 写开发日志 devlog/2026-06-26.md

## 遇到的问题
- AudioContext 拼接需要先解码 MP3 → 处理静音间隔 → 编码 WAV，流程较长但离线可行
- 逐句模式下生成 TTS 有轻微延迟（200-500ms），已用 loading spinner 缓解
- Azure 免费层有并发限制，逐句生成时加了 200ms 间隔避免 429

## 下一步
- 最后一步（第 8 步）：打磨 — 历史记录、进度恢复、安装引导、错误边界
