# 开发日志 — 2026-06-24

## 今日完成
- [x] `src/hooks/useVideoSync.ts` — 视频-字幕同步 hook
  - 监听 `<video>` 的 `timeupdate` 事件
  - 按时间戳匹配当前句子（`seg.start <= currentTime < seg.end`）
  - 当前句变化 → `scrollIntoView({ behavior: 'smooth', block: 'center' })` 自动滚动
  - 手动滑动暂停自动跟随，松手 3 秒后恢复
  - `jumpTo(index)` — 点击某句 → 视频跳转对应时间 + 立即恢复跟随
  - `registerSentenceRef` — DOM 引用注册，用于 scrollIntoView
- [x] 学习页 LearnPage 完整重写：
  - **字幕同步高亮**：当前播放句子 → 蓝色左边框 + 蓝色背景 + "▶ 播放中" 脉冲动画
  - **点击跳转**：点击任意句子 → 视频跳转到该句起始时间
  - **临时选中**：点击后显示 "🔊 已选中" 标记
  - **三行对照**：时间戳·普通话 · 粤语 · 拼音
  - **底部模式切换**：整段播放 / 逐句播放 分段控件
  - **播放控制栏**：⏮ 上句 / ▶ 播放 / ⏭ 下句（← → 已绑定 jumpTo）
- [x] TypeScript + Vite build 通过（47 modules → 387KB，0 错误 0 警告）

## 待办事项
- [ ] 第 7 步：Azure TTS 音频播放器
  - [ ] 完善 azure-tts.ts：SSML → MP3
  - [ ] AudioPlayer 播放控制 hook
  - [ ] 整段模式：所有句子拼接 + 静音间隔
  - [ ] 逐句模式：点哪句播哪句
  - [ ] TTS 音频缓存
  - [ ] 写开发日志 devlog/2026-06-25.md

## 遇到的问题
- 无重大阻塞。`scrollIntoView` 在 iOS Safari 表现略有延迟，已通过 `smooth` 行为缓解
- 需要在 iPhone 真机上测试同步精度（`timeupdate` 频率约 250ms）

## 下一步
- 第 7 步：Azure TTS — 粤语朗读（核心体验闭环的最后一步）
